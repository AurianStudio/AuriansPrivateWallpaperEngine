<Page
    x:Class="Lively.UI.WinUI.Views.Pages.Gallery.GalleryBrowseView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border
            Grid.Row="0"
            Padding="16,12"
            Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Spacing="4">
                    <TextBlock
                        FontSize="22"
                        FontWeight="SemiBold"
                        Text="Aurian Gallery" />
                    <TextBlock
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Text="Browse and download wallpapers directly from vsthemes.org" />
                </StackPanel>
                <HyperlinkButton
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Content="Open in browser"
                    NavigateUri="https://vsthemes.org/en/wallpapers/" />
            </Grid>
        </Border>

        <WebView2
            x:Name="galleryWebView"
            Grid.Row="1"
            Source="https://vsthemes.org/en/wallpapers/" />
    </Grid>
</Page>
